-- Hotel Billing Software
-- Create project database (run once as MySQL root / admin)

CREATE DATABASE IF NOT EXISTS hotel_billing
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE hotel_billing;
