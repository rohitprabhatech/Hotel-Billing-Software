# Local Vite talks to Flask via the /api proxy in vite.config.js (port 5003).
# Leave this empty in development so the proxy is used (avoids CORS and port mismatch).
# For production builds, set the full API origin, e.g.:
# VITE_API_BASE_URL=http://localhost:5003/api/v1
