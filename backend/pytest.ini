[pytest]
testpaths = tests
addopts = -q
markers =
    isolation: tenant isolation / IDOR regression probes (BIZ-64)
filterwarnings =
    ignore:Using the in-memory storage for tracking rate limits:UserWarning
